rules_version = '2';
// Arquivo temporariamente permissivo PARA DEBUG
// NÃO deixe essas regras em produção — elas permitem leitura e escrita para qualquer usuário.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
